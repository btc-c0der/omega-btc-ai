"""
Component tests for the Bitget position analyzer.

These tests validate that individual components work together
correctly within their bounded contexts, but without requiring
external dependencies like the actual exchange API.
""" 