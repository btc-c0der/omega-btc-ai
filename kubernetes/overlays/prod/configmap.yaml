apiVersion: v1
kind: ConfigMap
metadata:
  name: omega-common-config
data:
  APP_VERSION: "0.7.2"
  DEBUG_MODE: "false"
  REDIS_TIMEOUT: "10"  # Increased timeout for production
  ENABLE_METRICS: "true"  # Enable metrics collection in production 